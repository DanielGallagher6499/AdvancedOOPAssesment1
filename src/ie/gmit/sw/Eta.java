package ie.gmit.sw;

/**
* This is interface Eta. Eta contains a method which is overridden depending on
* which class is being used by status. If slight or low Beta will override this,
* if it's status medium Gamma will override this
*
* @author Daniel Gallagher
* @version 1.0
*/
public interface Eta {
	//Method which is being overridden by it's classes.
    public String Eta ();
    
    
}